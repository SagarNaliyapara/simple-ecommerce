<span>
    @if($count > 0)
        <span class="inline-flex items-center justify-center rounded-full bg-red-600 px-2 py-1 text-xs font-bold text-white">
            {{ $count }}
        </span>
    @endif
</span>
